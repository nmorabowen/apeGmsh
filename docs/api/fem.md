# FEM Broker — `FEMData`

Solver-agnostic snapshot returned by `g.mesh.queries.get_fem_data(dim)`.
Composite of `.nodes`, `.elements`, `.info`, `.physical`,
`.mesh_selection`, `.loads`, `.masses`, `.constraints`.

::: apeGmsh.mesh.FEMData
