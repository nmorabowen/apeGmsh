# Architecture Decision Records (ADRs)

Each ADR captures one significant decision: the **context** (what
was at stake), the **decision** itself, the **alternatives** we
considered and rejected, and the **consequences**.

ADRs are append-only. If a later decision reverses or amends an
earlier one, write a new ADR that supersedes it; do not edit history.

| # | Title | Status |
|---|---|---|
| [0001](0001-decouple-from-gmsh-session.md) | Decouple the bridge from the gmsh session | Accepted |
| [0002](0002-typed-primitives-with-capabilities.md) | Typed primitives carry capabilities | Accepted |
| [0003](0003-namespace-api.md) | Namespace API + static typing | Accepted |
| [0004](0004-section-separated-from-material.md) | Sections live outside `material/` | Accepted |
| [0005](0005-patterns-explicit.md) | Patterns are explicit context managers | Accepted |
| [0006](0006-class-name-apesees.md) | Bridge class is `apeSees` | Accepted |
| [0007](0007-time-series-separated-from-pattern.md) | Time series live outside `pattern/` | Accepted |
| [0008](0008-three-emit-targets.md) | Three emit targets via Emitter Protocol | Accepted |
| [0009](0009-no-backwards-compat-with-solvers.md) | No back-compat with `apeGmsh.solvers` | Accepted |
| [0010](0010-csys-for-frame-orientation.md) | Orientation fields for frame orientation (originally "csys") | Accepted |
| [0011](0011-h5-as-fourth-emit-target.md) | HDF5 as a fourth emit target | Accepted |
| [0013](0013-records-in-mesh-not-solvers.md) | Resolved records live in `apeGmsh.mesh.records`, not `apeGmsh.solvers` | Accepted |
| [0014](0014-viewer-is-pure-h5-consumer.md) | `apeGmsh.viewers` is a pure `model.h5` consumer | Accepted |
| [0015](0015-label-pg-separate-registries-kernel-leaf.md) | Tier-1 labels / Tier-2 physical groups are separate registries; `apeGmsh/_kernel` is a downward-only leaf | Accepted |
| [0016](0016-selection-unification-v2-complete.md) | selection-unification-v2 complete: legacy surface removed, two terminals on one spatial kernel, two ratified capability gaps | Accepted (supersedes the P2-I-transient framing of 0015; §4 amended by 0017) |
| [0017](0017-selection-gaps-are-incomplete-unification.md) | The two v2 capability gaps are *incomplete unification*, not accepted permanent gaps — v2-native successors owed/planned | Accepted (amends 0016 §4) |
| [0018](0018-modeldata-vanilla-opensees-enrichment.md) | `ModelData` — declarative `model.h5` orientation enrichment for hand-written OpenSees | Accepted (complements 0011) |
| [0019](0019-opensees-model-read-side-broker.md) | `OpenSeesModel` — read-side broker for `model.h5`, distinct from `apeSees` | Accepted (complements 0011 and 0018) |
| [0020](0020-results-carries-opensees-model.md) | `Results` carries `OpenSeesModel` via the Composed-file pattern; viewer stays file-mediated | Accepted (preserves 0014) |
| [0021](0021-lineage-chain-replaces-snapshot-id.md) | Lineage chain replaces `snapshot_id` binding; warn-not-raise | Accepted (ratifies the May 2026 bind-contract decision) |
| [0022](0022-mp-constraint-emission-fanout.md) | MP constraint emission via `/neutral/` fan-out — closes the §3.3 deferral | Accepted (widens the `Emitter` Protocol) |
| [0023](0023-per-zone-schema-versioning.md) | Per-zone schema versioning; two-version reader window | Accepted (replaces single-envelope policy) |
| [0024](0024-emitter-protocol-widen-region.md) | `Emitter.region` Protocol widening for MPCO `pg=` filtering | Accepted (widens the `Emitter` Protocol; schema 2.8.0 → 2.9.0) |
| [0025](0025-emitter-protocol-widen-eigen.md) | `Emitter.eigen` Protocol widening for one-shot modal solves; `apeSees.eigen` bridge method + `EigenResult` | Accepted (widens the `Emitter` Protocol; no schema bump) |
| [0026](0026-h5modelreader-protocol-contract.md) | `H5ModelReader` Protocol — formalise the viewer-side model read contract; unblock director path-survivor elimination and foreign-format adapters | Accepted (May 2026; successor to 0014/0019/0020; adoption via PR7-a through PR7-d, shipped in PRs #282/#286) |
| [0027](0027-cross-partition-mp-constraints.md) | Cross-partition MP-constraint emission policy: replicate on every owning rank, broker-deterministic phantom tags, auto ParallelPlain + Mumps | Accepted (extends ADR 0022; schema 2.9.0 → 2.10.0 in P4) |
| [0028](0028-initial-stress-via-parameter-ramping.md) | Initial-stress injection via parameter ramping (Phase SSI-1): `ops.initial_stress(...)` + Protocol `addToParameter` / `step_hook_ramp` + `analyze` hook-wrap | Accepted (widens the `Emitter` Protocol; H5 archival deferred) |
| [0029](0029-staged-analysis-context-manager.md) | Staged analysis via `ops.stage(name)` context manager (Phase SSI-2.A): per-stage analysis chain + Protocol `stage_open` / `stage_close` + `loadConst`/`wipeAnalysis`/hook-list cleanup contract | Accepted (widens the `Emitter` Protocol; live execution + H5 archival deferred) |
| [0030](0030-stage-bound-topology-activation.md) | Stage-bound topology activation via `s.activate(pgs=)` (Phase SSI-2.B): per-PG ownership + Protocol `domain_change` + lowest-stage-index node ownership + cross-stage tag-identity preservation | Accepted (widens the `Emitter` Protocol; extended in Phase SSI-2.C / PR #315 with `_emit_stages_partitioned` for MP + staged combo) |
| [0031](0031-ssi-convenience-helpers.md) | SSI convenience helpers (Phase SSI-3): `convergence_confinement` (wraps `initial_stress`) and `imposed_displacement` (wraps `pattern.Plain` + `sp`) | Accepted (no Protocol widening — composes over existing primitives) |
| [0032](0032-explicit-only-per-node-ndf.md) | Explicit-only per-node `ndf` via top-level `g.node_ndf` composite | Accepted (May 2026; codifies the doctrine shipped in PR #317 and hardened by PR #321) |
| [0033](0033-s2-emit-wiring-per-node-ndf.md) | S2: Wire per-node `ndf` into OpenSees emit paths (override-only) | Accepted (May 2026; PR #325) |
| [0034](0034-stage-bound-bcs-and-recorders.md) | Stage-bound BCs and recorders (Phase SSI-2.D): `s.fix` / `s.mass` / `s.region` / `s.recorder` builder methods + five-validator surface (H1 / V1 / V2 / V3 / V4) + recorder claiming + per-stage region tag cache | Accepted (May 2026; PRs #323 / #324 / #326) |
| [0035](0035-asd-embedded-node-element-option-exposure.md) | `ASDEmbeddedNodeElement` C++ optionals (`-rot` / `-p` / `-K` / `-KP`) exposed end-to-end through the constraint pipeline | Accepted (schema 2.12.0; widens the `Emitter` Protocol) |
| [0036](0036-embedded-host-decomposition.md) | Embedded-host decomposition: linear coupling over corner nodes for quad / hex / prism / pyramid / higher-order hosts; reserved `host_coupling` keyword; four-mitigation invariant set | Accepted (May 2026; no OpenSees changes, no H5 schema bump) |
| [0037](0037-higher-order-line-broker-split.md) | Higher-order lines: broker-side split via mesh editing (`split_higher_order_lines`); bridge surfaces untouched | Accepted (May 2026; no OpenSees changes, no H5 schema bump) |
| [0038](0038-compose-model-composition.md) | `g.compose()` model composition: flat-after-merge with namespaced tag-offset import | Accepted (2026-05-26; Phase 3, builds on the three-broker refactor 0019–0023) |
| [0041](0041-chain-phase-geometry-constraints.md) | Chain-phase routing for geometry-intensive constraints (`embedded`, `tied_contact`) | Accepted (2026-05-27; 8 open questions resolved) |
| [0042](0042-render-backend-seam.md) | Rendering-backend seam: `SceneLayer` IR + `RenderBackend` Protocol — render-side mirror of ADR 0026; decouples domain logic from PyVista so `trame` web/Jupyter (and a future ParaView export adapter) drop in behind one seam | Accepted (May 2026; R-A → R-C shipped, R-D in ADR 0047) |
| [0043](0043-connectivity-graph-and-flexible-emit.md) | Connectivity graph (parts=nodes, interfaces=edges) + flexible split emit (driver + per-part sourced fragments) for model chaining; phase-bucket order as the unified deck/compose contract; monolithic (A) vs sequential/Workbench (B) coupling | **Proposed** (discussion draft; open questions drive the agenda) |
| [0045](0045-selection-and-pick-contract.md) | Selection & pick contract: `SelectionTarget` IR + `Substrate` + canonical `BBox` + `FilterController` (the `0/1/2/3/4` dim spine) + `SelectableSubstrate` Protocol + serialized `SelectionLog` over an additively-widened `PickBackend` — unifies picking across the model/mesh/results viewers, fixes volume picking + the six-bbox mess, lands above ADR 0047 | Accepted (2026-05-30; from a 13-agent design workflow; 4 forks ratified; slices S0 → S6) |
| [0047](0047-pick-backend-and-export.md) | `PickBackend` Protocol (optional capability gated by `supports_picking()`) + VTK-free pick IR + server-side web picking + `ParaViewExportBackend` — Phase R-D of the render seam; brings the last VTK-bound domain surface (picking) behind a Protocol | Accepted (2026-05-30; 4 open questions resolved; R-D.1 + shared backend shipped in #480) |
| [0050](0050-dimension-indexed-loads-and-displacements.md) | Dimension-indexed `g.loads` + a `g.displacements` composite (authoring surface) | Proposed (2026-05-31; P1–P3 shipped via #493; §5 element-form + LOAD-1 auto-emit superseded by 0051) |
| [0051](0051-bridge-load-consumption.md) | Bridge load consumption: opt-in `from_model` import (no auto-emit), load **cases** on the geometry vs **patterns** on the bridge, all-nodal, stage-scoped patterns + two-mode no-mixing rule, `remove_bc` alias (the §7 unconsumed-case reconciliation warning was implemented then removed — the deck is authoritative) | Proposed (2026-05-31; supersedes ADR 0050 LOAD-1 + §5 emit; masses/constraints import = follow-up round) |
| [0052](0052-staged-reference-position-contract.md) | Staged reference-position contract: new `s.support` (HOLD-at-current via `-const sp` in a per-stage constant pattern) vs `s.fix` (ANCHOR-to-t0, unchanged); base-build keeps `fix`, MP untouched; frame birth (Route A) = `useInitialDisp` + commit-first; continuum birth (Route B) deferred to the per-GP `F₀` gradient wrapper with a fail-loud guard (global `InitialStateAnalysis` forbidden — corrupts equilibrated elements) | Proposed (2026-06-01; extends 0034; source-cross-checked against the fork `SRC/`) |
| [0053](0053-damping-definition.md) | Damping definition: one `ops.damping` namespace + a flat `ops.eigen`; every member a lowercase declaration with a uniform `on=` scope (no `assign`, resolve-at-emit); global + region-scoped Rayleigh (raw + ratio helper, `stiffness` default `initial`), `modal` (eigen-backed; `modalDampingQ` dropped as a verified anti-damping bug), object-backed `uniform`/`sec_stif`/`urd`/`urd_beta` (+ `region -damp`, time-window `activate_time`/`deactivate_time`/`factor`→`-factor`); owns 4 of OpenSees' 6 damping channels | Proposed (2026-06-01; source-cross-checked vs upstream + fork `12_damping_channels.md`; all 4 design Qs resolved) |
| [0056](0056-viewer-state-and-event-contract.md) | Viewer state & event contract: single owners (intent vs effective vs widget-projection), owner-fired dispatcher events, dispatcher-always (no fallback renders), reconciler-only artifact writes, AST-guard enforcement (G-RENDER / G-ARTIFACT / G-IMPORT in `ui/`); mesh + model viewers join the same contract | Proposed (2026-06-10; V0 surgical pre-slice shipped in #593) |
| [0057](0057-solution-strategy-ladder.md) | Solution-strategy ladder: opt-in `ops.strategy.Ladder` + established `profile()` presets (`"standard"`, `"non-smooth"`, `"smooth-hardening"`, `"penalty-stiff"`, `"exhaustive"`) emitted into the #587 per-increment loops — per-increment algorithm escalation with reset-on-success, `Substep` with exact-λ landing for load stages, loud rung provenance, fail-loud exhaustion; no tolerance relaxation ever | Proposed (2026-06-10; evidence = zoned-twin campaign: line search measurably *inverts* the folk robustness ordering on MC plasticity) |
| [0058](0058-concurrent-geometries.md) | Concurrent geometries: a viewer `Geometry` becomes a scene instance (own `FEMSceneData` + deform state + `visible` + offset + optional stage pin), all visible instances render and "active" demotes to editing target; per-geometry DEFORM pump; `sync_substrate_points` becomes universal (`DeformedShapeDiagram` retired, exemption list deleted); declarative diagram-kind registration | Accepted (2026-06-12; slices S0–S4 all shipped #623–#651; builds on the PR #620 deform-follow guard + ADR 0056) |
| [0059](0059-mesh-recipes.md) | Mesh recipes: one-call unstructured / structured meshing via a new `g.mesh.recipe` sub-composite — scope = whole model or label/PG/part region; field-based (`Constant`+`Min`) region sizing instead of bleeding point-`lc`; `fallback="unstructured"` for non-hex-decomposable entities; fail-loud mixed quad/tri interface guard (recipe path only, never raw `generate()`) | Proposed (2026-06-11; pure orchestration over existing `sizing`/`field`/`structured`/`generation` verbs — no schema change) |
| [0060](0060-remote-hpc-submission.md) | Remote HPC job submission `apeGmsh.hpc`: standalone `Cluster`/`Job` primitives owning push → sbatch → poll → fetch over native ssh/scp (BatchMode, zero new deps); two-layer config (`~/.ssh/config` owns the connection, `~/.apegmsh/clusters.toml` owns cluster facts); generated LF `job.sbatch` next to the deck; `.exit_code` sentinel replaces broken `sacct`; JSON sidecar makes `Job.load(dir)` survive sessions | Accepted (2026-06-11; run-verified e2e on Esmeralda; bridge sugar `p.run_remote` deferred) |
| [0061](0061-per-rank-deck-emission.md) | Per-rank Tcl deck emission `apeSees.tcl(per_rank=True)`: observation-only span recording in `partition_open`/`partition_close` + a driver that `source`s rank-local fragments (`ranks/rank<K>_<seq>.tcl`) — per-rank parse drops O(model) → O(global + model/np), killing the measured Amdahl serial term (deck parse) and the 16-ranks/node deck-text RAM pressure; HPC layer untouched (rank-axis sibling of ADR 0043 split-emit) | Accepted (2026-06-12; impl #641; bench 143849–143852 **refuted the parse-time premise** — 130 ms not 5 s at 66k; decision stands on resident-text RAM + NFS volume + Tcl 2 GB ceiling, all O(model)→O(model/np)) |
| [0062](0062-moment-tensor-equivalent-body-force-source.md) | Moment-tensor equivalent body-force source: embed a seismic double-couple (or an FFSP finite fault) inside a solid mesh as RHS nodal forces $F^a=M\!\cdot\!\nabla N_a$ modulated by a normalized moment function `Path` — bridge-authored `p.moment_tensor` + `ops.fault.from_shakermaker` (one pattern/source), consistent (∂N/∂x, reuses ADR 0036 host finder) + dipole methods, explicit A&R-down↔mesh-up frame contract + `region=` guard, validated against the analytic double-couple radiation (≈0.97); no element, no solver change (RHS-only → explicit-friendly); **ShakerMaker is NOT a dependency** — plain-dict/duck-typed interchange | Proposed (2026-06-17; MT-1→MT-3, MT-4a, MT-5a/b shipped; `from_ffsp` corrected-units + FK waveform overlay owed to a ShakerMaker run) |
| [0063](0063-split-node-kinematic-fault.md) | Split-node kinematic slip fault: put an actual fault **surface** in the mesh as a discontinuity — `g.mesh.editing.split_fault` duplicates the surface nodes into +/− sheets (welded tip), and a kinematic driver prescribes the relative displacement (slip) $\Delta u = s\,\hat d\,S(t-t_r)$ via TSN forces / constraints; shares ADR 0062's FFSP slip data + `Yoffe` STF but applies it **on** the fault (near-field) not as a body force (far-field). Spontaneous (friction-law) rupture deferred — this builds its meshed-discontinuity substrate | Proposed (2026-06-17; design draft, for later; the heavier sibling 0062 deferred; mesh-split + slip realization owed source-verification) |
| [0064](0064-ladruno-recorder-region-filter.md) | Ladruno recorder region filter (`-R`): give `Ladruno` MPCO's region-targeting surface (`nodes=`/`nodes_pg=`/`elements=`/`elements_pg=` → auto-emitted `region` + `-R $tag`) by extracting a shared `FilterableRecorder` base from MPCO and broadening the ADR 0027 INV-4 partition gates `isinstance(…, MPCO)` → the base; `-R` emitted before the trailing `-G energy`; `region=` + `energy=True` fail-loud for now (per-region energy `-G energy <tag>` stays deferred, landing reserved). Bridge-only — fork recorder already parses `-R`; no schema bump | Accepted (2026-06-18; RF0–RF3 shipped, full opensees suite green; mirrors 0024/0027; plan = `internal_docs/plan_ladruno_region_filter.md`) |
| [0065](0065-streaming-deck-emission.md) | Streaming deck emission: a write-through `write_to` sink removes the author-side `list[str]` line buffer — all monolithic and fragment/driver emit sites stream their buffer instead of materializing a joined deck-sized string (byte-identical, suite-verified); the dual-mode sink + per-rank live routing (Tier 2) is demand-gated on a `tracemalloc` profile of a real OOM model. The "next ceiling" deliberately deferred by ADR 0061 | Accepted in part (2026-06-18; Tier 1 streaming shipped; Tier 2 demand-gated) |
| [0066](0066-h5drm-drm-authoring.md) | H5DRM DRM-load authoring: typed `ops.pattern.H5DRM` emit (factor/`crd_scale`/dist_tol/transform/x0 — field-carrying, owns the km→m + centred-frame handshake) + `g.parts.add_DRM_box_from_h5drm` (dataset-keyed structured-hex box whose nodes land on the `.h5drm` stations) + `g.drm_buffer` (exterior buffer on sides+bottom + `fixed`/`lysmer`/`asd` boundary, with the **all-nodes-DRM exclusion invariant** so buffer/boundary nodes stay non-dataset — H5DRMLoadPattern.cpp:580). Encodes the z-down / `crd_scale` / 3-DOF≤8-node / free-box-diverges / hold-past-tend pitfalls. Pairs with ADR 0054 (absorbing) + the parametric `DRMBox` part | Proposed (2026-06-19; REQUIREMENT; slices D-1 pattern → D-2 builder → D-3 buffer+fixed → D-4 asd; reference impl + validation = OpenSees fork `drm_study` / PR #296) |
| [0067](0067-reinforcement-cage-authoring.md) | Reinforcement-cage authoring: a new `g.rebar` geometry composite (L1 frozen specs `Hook`/`Path`/`Bar`/`Stirrup`/`Cage` + `BarLayout`/`TieLayout`/`DetailingStandard`/`BarCatalog`, L2 composite + standardized `column`/`beam` generators, L3 fluent `BarBuilder`) sitting **above** the shipped `g.reinforce` — authors arbitrary polyline cages with code-aware (ACI 318) hooks/bends, polyline+metadata by default (true-arc opt-in), and **delegates** coupling: inline conformal via `embed` (shared-node perfect bond, MPI-OK) or inline/composed embedded via `g.reinforce` -> `LadrunoEmbeddedRebar`; conformal-across-Part raises; composed-Part H5 + beam-element + twist gated (P5). Built P0-P4 with 3 adversarial gates, 101 tests | Proposed (2026-06-20; design wf wf_7e15f74c-6e2; no C++/inline-schema change) |
| [0068](0068-equation-constraint-tied-interface.md) | Constraint-based non-matching tie via `equationConstraint` (EQ_Constraint): a second `enforce=` route on `tie`/`tied_contact` (`"penalty"`→`ASDEmbeddedNodeElement` unchanged, `"penalty_al"`→`LadrunoEmbeddedNode`, `"equation"`→`EQ_Constraint`) reusing `resolve_tie`'s projection+weights wholesale; a sixth `Emitter` Protocol method `equationConstraint`; per-DOF expansion `u_d(slave)=Σ wᵢ·u_d(mᵢ)`; EQ-aware handler auto-emit (`Lagrange` implicit / fork `LadrunoProjection` explicit — never `Transformation`); exact + Δt-neutral + tie-force-recoverable, the explicit-DRM (ADR 0066) non-matching-interface enabler. `EQ_Constraint` is upstream (Huang 5/2025); fork-only = `LadrunoProjection` + `"penalty_al"` | Proposed (2026-06-20; extends ADR 0022; LS-DYNA `*CONTACT_TIED_SURFACE_TO_SURFACE` constrained-variant analogue) |
| [0072](0072-etabs-bridge-decompose-build.md) | Decompose `build_opensees` into composable public emit steps (`inject_diaphragms` / `emit_elements` / `emit_supports` / `emit_loads` / `emit_mass`) with `emit_elements(skip=/only=)` — the ETABS import is a regenerable *scaffold*; the bridge owns the **mechanical** deck (fixities/springs/loads/mass/diaphragms) + an elastic **default**, and hands off **labeled geometry** (PGs by section, joint ids) so native `apeSees` (`pg=`), not an overlay DSL, is the **modeling-control** surface. `build_opensees` becomes a byte-identical wrapper. Stiffness modifiers = orthogonal faithfulness workstream | Proposed (2026-06-25; interop refactor, no schema/Protocol change; sibling of 0051, shape of 0059) |
| [0075](0075-modal-response-family.md) | Consuming the Ladruno modal-analysis family (fork ADRs 43/44/45/46): `modal_properties` Protocol widening + `ModalPropertiesResult` (PR 1); `modal_response_history` / `response_spectrum_analysis -combine` as deck-emitting Protocol methods with auto eigen+modalProperties prerequisites (PR 2); live-only `frequency_response` / `steady_state_dynamics` / `random_response` drivers (PR 3); `eigen_feast` band-form Protocol method reusing `EigenResult` (PR 4); live-only `complex_eigen` + `ComplexEigenResult` (PR 5). Explicit per-call damping channels (`_damping_channel_args`, no `ops.damping` coupling), handle→tag excitation resolution, staged models refused, H5 no-ops everywhere (no schema bump) | Accepted (2026-07-13; five PR slices) |
| [0074](0074-ladruno-up-porous-element.md) | `ops.element.LadrunoUP(pg=, material=)` — the fork's unified Biot u-p saturated-porous continuum element (ELE_TAG 33017, fork ADR-71): one typed class, shape from the mesh (D1); heterogeneous intra-element ndf via per-slot machinery keyed on node count with strict `{floor}` per slot (D2); straight-side pre-validation (D3) + solver allow-list guard (D4) both gating in `emit()`; ndm-aware builder gate; fork capability gating (D5); `-body` values are ACCELERATIONS | Accepted (2026-07-18; doc #789, shipped #793 + fork #574) |
| [0077](0077-parallel-modal-analysis.md) | Parallel modal analysis on the partitioned / HPC path. **Plain `eigen` over `MumpsParallelSOE` under `OpenSeesMP.exe` was REFUTED by adversarial review** (global-K/local-M incoherent → per-rank-local garbage modes; the SP/MP non-composition FEAST exists to fix; apeGmsh already fails loud on it, `apesees.py:4285`). Reworked to two tiers: **Tier 0** serial-gather stopgap (existing serial `eigen`/`modalProperties` on the unpartitioned build — correct for node-sized models, does NOT scale the eigensolve); **Tier 1** distributed **FEAST** `eigen -feast fmin fmax -rci [-certify]` (fork ADR 43, L3-only) via a **runtime-agnostic** driver reaching FEAST through EITHER a PyMP `.py` deck (2a) or fork classic-Tcl `-feast` parity (2b) — classic `commands.cpp` has no `-feast`. Harvest fixes: single-capture eigenvalue write-out (no double/deadlock solve), MPCO/`.ladruno` HDF5 eigenvector recorder so the node-id merge applies (plain `.out` rejected), forced `constraints Transformation`+`system Mumps` preamble. `modalProperties` MPI-blind → deferred + raised-on. Rides ADR 0060/0061 | Proposed (2026-07-15; v1 refuted; Tier 0 P0, Tier 1 FEAST P1–P5) |
| [0073](0073-contact-generator.md) | `g.constraints.contact(master, slave, formulation="nts"\|"mortar", …)` → fork `contactSurface` + `contact` + `LadrunoContact` handler: NTS penalty / mortar-ALM face-to-face contact (+ Coulomb/Tresca friction, mesh-tie). Resolve drops higher-order faces to corner facets (nps∈{3,4}); outward is **never auto-derived** (the fork computes per-facet normals; a single `-outward` only pins the sign and is wrong on curved/closed masters) — passed only when explicit, and **required** for a `tie=True` mesh-tie (else fork gate H2 silently zeros it). Fail-loud on contact under partitioned emit, contact+equation-tie, and contact+handler-requiring-MP. H5 persistence deferred (`H5FeatureDeferredWarning`); `-soft`/`-visc`/`-consistanttan`/`-geomtan`/edge-edge/`contactPlane` deferred. Hardened by an ad-hoc 3-agent + a 30-agent adversarial-review workflow | Accepted (2026-06-25; #722/#723 content; ADR-number bumped 0072→0073 after #739 took 0072) |
| [0078](0078-section-properties-analyzer.md) | In-process cross-section property analyzer: standalone `SectionProperties(fem)` broker (sibling of `Results`/`apeSees`, living in `apeGmsh/sections/` next to the solid builder) computing geometric (modulus-weighted composite via PG→`SectionMaterial` map), Saint-Venant warping (`J`/shear centre/`Γ`/`As` — scalar Laplacian over `apeGmsh.fem` shape functions + `scipy.sparse`, Lagrange-multiplier regularization), plastic (bisection, `fy`-weighted), and stress recovery (matplotlib-only; viewer out of scope per 0014) on any meshed 2-D face (tri/quad, holes, multi-material; tri3 warping warns). Authoring-axes results = PyPI `sectionproperties` convention; one shared lowering owns the x≡z/y≡y OpenSees mapping (`Ixx→Iz`, `As_y/A→alphaY`). **Declarative bridge binding**: `ops.section.ComputedSection(analysis=sec)` subclasses the `Section` base → slots into every `section=` consumer, resolves lazily at emit to a plain `section Elastic` line (no Protocol widening, no schema bump; `kind="fiber"` lowering reserved; eager `to_elastic_section()` escape hatch). PyPI package = dev-only CI oracle, never a runtime dep; flat-face `g.sections` builders (`W_face`, …) in S5. Full API contract in-ADR: frozen `GeometricProperties`/`WarpingProperties`/`PlasticProperties`/`SectionStress` with the **rigidity-form naming law** (`EIxx_c`/`GJ`/`Mp_xx` always valid; unprefixed `Ixx_c`/`J`/`Sxx` divide by the single modulus, raise `CompositeSectionError` + `transformed(e_ref)` on composites). Disconnected parts: `disconnected="raise"|"sum"` constructor policy (geometric/plastic connectivity-blind; warping default fail-loud catches unfragmented faces, explicit sum = per-part solves, `GJ=ΣGJᵢ` multi-girder lower bound; partial shear transfer authored via `SectionMaterial(G=)` override — tiny-E/calibrated-G equivalent strip, never a coupling knob). S6 = standalone Qt+matplotlib **section inspector** (`sec.viewer()`, live load inputs blending 6 unit stress fields; deliberately NOT the 0014/0042 viewer family) + Jupyter `_repr_html_` | Accepted (2026-07-18; slices S1–S6 = #802/#803/#804/#805/#808/#810, gates G-A/G-B/G-C passed; first in-process solve — ADR 0009 scope clarified) |
| [0079](0079-documentation-architecture.md) | Documentation architecture: one didactic site (`docs/` = the only published surface), four doc tiers (learning site / ADR design record / `internal_docs` working memory / derived AI skills), the single-home rule (one fact, one canonical page per Diátaxis mode) + the course-not-archive rule (nav order = reading order, ~60-page cap, one human prose-first voice, distillation = rewrite never splice, `docs_style.md` contract), dissolve the `architecture/` root, un-publish plans/guides/`first_steps`, add a Learning-path page + curated showcase animations (hero loop + per-flagship-example loops via `export_animation`, ≤ 15 MB budget), gate with `mkdocs build --strict` | Accepted (2026-07-18; executed same-day — P0 #817 / P1 #819 / P2 #821+#824+#825 / P3 #827 / P5 #828; 18 guides + first_steps + 21 architecture notes absorbed into ~57 curated pages; strict gate live; P4 skill re-sync = follow-up; plan = `internal_docs/plan_docs_architecture.md`) |
| [0081](0081-legend-is-a-view-annotation.md) | The colour scale is a **view annotation owned by the viewer**, not a diagram property: `LegendController` (sibling of `LUTManager`) owns one `LegendEntry` per coloured array — anchor / extent / orientation / `font_scale` / visibility are app state that survives every recolour, step change and re-attach. Kills the five state homes (style dataclass, mixin `_runtime_*`, dead `LUT.show_scalar_bar`, actor geometry, widget geometry), the two identity keys (`layer_id` vs on-demand title + `_bar_prefix_resolver`) and the two divergent layout implementations (`_legacy_layout_kwargs` vs `_scalar_bar_layout`). Box extent is **derived from its content** (font metrics × tick count × `fmt`), so "too small for its text" and "title off-viewport" become unrepresentable; controller-side slot allocation replaces pyvista's (permanently off, since we always pass explicit positions). `vtkScalarBarWidget` is **dropped, not unblocked** — the pick engine aborts every plain LMB press at priority 10 and the widget observes at 0.5, so the handle was unreachable by construction; drag / resize / snap / re-dock / context menu move into a `LegendInteractor` at priority 12 that aborts only on a box hit. Tests assert pixels and stickiness (L-GEOM / L-FIT / L-NOOVERLAP / L-STICKY / L-EVENTS / L-PICK), not VTK call-throughs | Accepted (2026-07-25; ratified 2026-07-24; lands under 0056, constrained by 0042; forks F1 drop-the-widget + F2 full runway + all 3 open questions resolved; **L0–L3 all shipped** — controller, content-derived boxes, slot allocation, backend reduced to a projection, one owner + dispatcher events + resize hook; drag/resize/snap/re-dock/context-menu on a priority-12 interactor that aborts only on a hit; placement persisted in the viewer session (schema v7→v8); web parity free (the legend lives in the scene IR, not a Qt overlay); ~115 tests incl. L-GEOM/L-FIT/L-NOOVERLAP/L-STICKY/L-EVENTS/L-PICK) |
| [0082](0082-analysis-chain-coherence-guards.md) | Analysis-chain coherence guards: the bridge refuses a chain OpenSees would silently rewrite. **G1** — model upstream's *two integrator slots* (`theStaticIntegrator` / `theTransientIntegrator`, chosen by C++ base class) and raise at any `analysis` whose slot was never filled, which is exactly where upstream substitutes `Newmark(0.5,0.25)` / `LoadControl(1,1,1,1)` behind a **suppressible** warning (`OpenSeesCommands.cpp:657/908`, same on the Tcl path) — the silent discard that blocks `LadrunoArcLength` push-to-failure, since the ignored arc-length yields a plausible curve that cannot pass the limit point. Slot table is source-derived, not intent-derived: `LadrunoDynamicRelaxation` is quasi-static in method but a `TransientIntegrator` by dispatch (fork ADR-21 refutes the static route). **G2** — `Newton(tangent="initial")` warns and names `ModifiedNewton(tangent="initial")`: same iterates, but `NewtonRaphson.cpp:163-191` re-assembles + re-factorizes an unchanged `K₀` every iteration (`zeroA` kills ADR-40c reuse). Warning + never a rewrite; whole-analysis `-factoronce` reuse **rejected** (`LadrunoArcLength` adapts `cOverDt` per step). Two bridge guards, no new knob, no fork change | Proposed (2026-07-25; **amended same-day by adversarial review** — the original set-membership G1 was probe-refuted as a false-positive on ordinary gravity-then-dynamics models, and the ADR-40c −57.8 % was an overclaim, now an A/B gate; fork hardening = named follow-up) |
| [0083](0083-results-viewer-section-planes.md) | Section planes for the results viewer: SketchUp-style interactive clipping as a **view operation owned by the viewer** — `ClipPlaneSetController` (≤ 6 active world-space planes + `apply_cuts`/`show_gizmos`, one `CLIP_PLANES_CHANGED` event per mutation, per 0056) crossing the seam as `RenderBackend.set_clip_planes` **stamping state** (backend gains the handle registry it lacked; every actor created later — incl. the `update_layer` rebuild path that would otherwise silently drop the cut — is stamped on the way in; labels exempt); off-seam substrate/wireframe/node-cloud stamped inside their creation helpers (both call sites); planes cut the *deformed* shape (points are rewritten per step); `vtkCellPicker` phantom-picks fixed by a post-pick clipped-side rejection; session v9 `clip_planes` (legends-pattern), layout schema 5→6; "section cut" name avoided (`clip_plane` / "Section planes"). Slices: S1 panel-driven dock (axis chips + bbox slider + unclamped numeric + flip), S2 priority-12 gizmo (0081 L2 pattern — stock `add_plane_widget` dead at 0.5 under the pick abort at 10), S3 optional contour-on-cut-face. Trame inherits server-side for free; 8 pre-implementation adversarial probes (3 draft defects folded in: rebuild-path loss, second substrate site, pick-through-the-cut) | Accepted (2026-07-28; **S1 #868 + S2 #870 shipped**; scope forks ratified: hollow-first / panel-first / desktop-first. S2 added the priority-12 gizmo — clip-exempt scene-IR quad + arrow, translate along the normal, trackball rotate via a `set_pose` mutator that re-anchors the pivot, zero actor rebuilds per drag. Three adversarial rounds found 7 defects, all fixed + mutation-checked + regression-tested: A1 duplicate plane ids after a sparse restore; B1 dead toolbar button (tabified-dock visibility feedback loop) / B2 planes-only sessions never restored / B3 numeric fields untypeable (UI-lane refresh ate keystrokes) / B4 add-at-origin blanked off-origin models; C1 drag gain exploded near face-on viewing (47.8 units per pixel on a 2-unit model) / C2 window cursor had two owners → `_cursor_arbiter`. Two live-window passes. **S3 contour-on-cut-face REQUIRED for solids, in progress** — the F1 'hollow first' trade was ratified against line/shell models and REFUTED on a real solid (6x4x3 box, 8968 tets): clipping discards fragments and a contour renders only the exterior skin, so a section plane on a solid opens into an EMPTY box — no field on the cut face, nothing in the interior. Sequencing decision, not a standing trade; sand is a workaround needing the substrate hidden) |
| [0084](0084-results-viewer-cascade-freeze.md) | Results viewer cascade freeze: one reconciler before catalog growth — finishes 0056 adoption in the results shell. D1 no new diagram kinds / region tools until the freeze criteria hold (render-count spy: one STEP + one DEFORM + one RENDER per scrub); D2 pumps-bound flag on `Dispatcher.bind()`, Director STEP/STAGE become intent+observer when bound (direct pump stays as the explicit unbound-branch contract for web/trame + bare-director), single pin-aware STEP absorbs the combined-mode global→local translation; D3 batches must leave no stale RENDER-lane state (hand-patch pattern retired); D4 the five silent pump sites go loud via the `_failures` registry + collect-at-teardown fixture (bare re-raise rejected — Qt event loop swallows); D5 `results_viewer.py` joins `_GUARDED_FILES` (budgets G-RENDER 9 / G-ARTIFACT 15 / G-IMPORT 7, two-way ratchet); D6 session deserialize pads `None` sentinels so `layer_indices` stay aligned (stable IDs rejected; schema stays v10); D7 headless interaction net + `PumpSet`/session-apply extraction promoted into the freeze. 10-PR dependency ladder in `internal_docs/plan_results_viewer_phase0.md`; single-STEP lands **last**, under the net. Evidence: external three-doc assessment verified by a four-agent adversarial sweep (two claims refuted: export does not blank without the dual path; the cited strict-env precedent does not exist) | Accepted (2026-07-31; forks ratified same day: F-A(a) id-coalesced RENDER-lane replay on batch exit, F-B(a) qt CI lane now. **All 10 PRs shipped in #880**; D1 freeze criteria met — scrub is 1 STEP + 1 DEFORM + 1 RENDER, catalog work unblocked. Found en route: the dual path was a CRASH not just double work (pinned short stage -> IndexError); GEOMETRY_ADDED had no substrate-sync subscriber, so multi-geometry restore landed invisible geometries; the qt lane found 3 never-run bit-rotted tests on its first run) |
| [0085](0085-meshable-part.md) | `Part` stays geometry-only; independently-meshed parts are the **compose seam**, not a `Part` capability. The mixed-order gap (hex20 ribs + hex8 covers — impossible in one session, `set_order` is global) is closed by the already-built route: one full `apeGmsh` session per part → `g.save()` → `from_h5` host + `g.compose` / `apeGmsh.assembly.Assembly`. Options (a) mesh composite on `Part` and (b) `MeshablePart`/`to_module()` rejected — both reverse ADR 0038's explicit `Part.bake()` rejection, fork the STEP/h5 persistence contract, and still can't author real parts (the production host part needed `g.displacements`, which `Part` lacks by design). Work items: D1 fix the stale `Part` docstrings (they deny the `physical` composite present since `ba4c6e45` and cite a deleted Assembly object), D2 `Assembly` gains `kind="tie"` (the calibrated interface — `enforce="equation"` exact at −0.01 %, penalty diverges in N/mm), D3 document the route + hazards (`get_fem_data(dim=None)`, PG-survival assertion), D4 acceptance test: hex20/tet10/hex8 parts composed + tied, closed-form series-stiffness check | Accepted (2026-08-04; signed off + implemented same day — Assembly kind="tie" landed, docstrings fixed, route documented, acceptance test measures −0.013 % vs the series closed form; builds on `fix/compose-pg-tag-collision` `7b63d67a`; driver = Cerro Lindo rung-4 solid fuse, 5 hand-rolled part sessions in production) |
| [0086](0086-mortar-tie-composed.md) | Integral-mortar mesh-ties on composed assemblies: mortar is a **weight method on `tie`** (`tie(method="mortar", enforce="equation")`), NOT a fourth enforce route and NOT contact — dual-basis mortar produces the same per-slave-node `InterpolationRecord` shape (`u_s = Σ P·u_m`), so ADR 0068's one-record/three-emissions invariant, the h5 schema, and the handlers all survive untouched. Weights computed in apeGmsh numpy (port of the fork `LadrunoMortarKernel` algorithm + its four guards, extended to tri6/quad8) because the fork's `LadrunoTie -mortar`/`contact -tie` paths hard-refuse nps∉{3,4} (`LadrunoTie.cpp:1191`) — the hex20 rib faces that motivate the feature cannot be expressed to the fork at all — and `contact` forces the `LadrunoContact` handler, which excludes the equation ties the same model needs (`apesees.py:5101`). Normals derived from master facet winding, ambiguity = hard error (`outward=` override); v1 scope flat/coincident/convex. Bundles the fix for `contact()`'s chain-phase silent drop (never calls `_add_def` — def sits inert) → `ChainPhaseError`. Acceptance: composed hex20/hex8 two-block rig, K=EA/L/2 at 0.1 %, order-swap symmetry asserted | Accepted (2026-08-04; signed off + implemented same day — D1 method= as recommended, D2 "both" (numpy now, fork quad8 kernel = named follow-up), D3 flat-coincident v1. Kernel 11 unit tests incl. quad8-on-quad4 linear patch at 1e-9; composed hex20/hex8 acceptance passes closed form AND order-swap symmetry at 0.1 %; rows survive the h5 round-trip; non-nested-rig discriminator guards against collocation fall-through (on nested grids dual mortar provably COLLAPSES to collocation — bilinear ⊂ quad8 span); contact() chain-phase silent drop now raises ChainPhaseError; 224+166 regression green; driver = Cerro Lindo fuse a=0.195→0.230 recovery without second-order plates) |
| [0087](0087-viewer-visual-design-system.md) | Viewer visual design system: `Palette` fields are the only color source (role table frozen; new fields must default), spacing/type/radius tokens split across their three existing homes (`Palette` / `LayoutMetrics` / `DensityTokens` — ownership table settles which), seven component invariants (INV-1 one-name docks, INV-2 honest empty states, INV-3 section pattern with header toggles, INV-4 one self-rendered icon factory — no bare-letter buttons, no icon library, INV-5 microcopy + File/View/Help, INV-6 themed-by-default under the three canonical palettes, INV-7 objectName↔QSS coupling), and four two-way-ratcheted style guards (G-HEX / G-SHOUT / G-QSS-SYNC / G-INLINE) in a `test_viewer_style_contract.py` sibling of the 0056 state guards. Chrome-side companion to `apeGmsh_aesthetic.md`; the standing rulebook for the two-phase GUI polish | Accepted (2026-08-04; evidence = `gui_audit` screenshot sweep + code sweep: 32 inline `setStyleSheet` sites / ~50 color literals outside `theme.py`; Appendix A = complete 40-glyph icon vocabulary + metaphor registry, Appendix B = target menu map per viewer) |
| [0088](0088-viewer-window-architecture.md) | Viewer window architecture (GUI polish Phase-2 blueprint): results viewer boots with THREE docks (Outline / Inspector / Time scrubber) instead of six; the five-tab rotated spine (Diagram/Geometry/Details/Display/Section planes) consolidates into ONE outline-selection-driven **Inspector** (`dock_results_inspector`) — Color Mapping dissolves into the diagram context (killing the worst INV-2 offender), Display and Section planes stay separate docks (not selection contexts) but default hidden; on-demand panels get defined summon paths (Plots auto-shows on first plot, Output = full-width bottom console below the scrubber via its status badge, Definitions/Display = View menu, Section planes = toolbar glyph); state rules: schema 6→7 exactly once, retired objectNames never reused, Reset layout restores the three-dock boot, Ctrl+H focus mode keeps viewport + scrubber + HUDs; 13 numbered acceptance criteria for Phase 2 | Accepted (2026-08-04; design blueprint — evidence = `gui_audit`/`gui_after` capture sets; resolves the Grok G1.1 inspector question + the Diagram double-header/clipped-hint and Output-beside-scrubber defects; mesh/model target partitions declared in D5, scheduled later) |
| [0089](0089-viewport-model-presentation.md) | Viewport model presentation for the results viewer: a three-role line-width hierarchy (2.5 px feature/silhouette outline via static feature edges — NEW actor / 1.0 px interior mesh edges — `mesh_line_width` default drops from 3.0 / ≥2 px overlay lines), node cloud **off by default at boot** (was on, 10 px black — and re-wired from the BRep `point_size` to `node_marker_size`), substrate colors become per-theme roles (Paper gets `#d9d9d9` fill; edges near-black under a new `SUBSTRATE_AMBIENT = 0.35` lift that fixes the measured 72–88 RGB shading floor that made `#444444` edges invisible), edges demote to 40 % `outline_color` while a field diagram is active, `ContourStyle.cmap` `"jet"` default closed. Amends `apeGmsh_aesthetic.md` §§2.2–2.3/5.2; viewport-side companion to ADR 0087 | Proposed (2026-08-04; A/B evidence = 3 rendered comparison sheets on `column_demo.h5`, mutated-actor GL captures; rendering changes await sign-off) |
| [0090](0090-scalar-bar-design.md) | Scalar bar design (the viewport colormap legend): text routed from chrome roles — labels/title in `palette.text` retinted live via a `set_theme_colors` controller mutator carried on `ScalarBarSpec` (kills the measured E1 order bug: first window renders BLACK bar text because `viewer_window.py` sets the pyvista theme AFTER constructing the `QtInteractor`), mono (Courier) tick labels, designed titles (`displacement_z` -> "Displacement Z"; key/persistence untouched), `fmt` default `%.3g` -> `%#.3g` (aligned trailing-zero columns; printf `g` IS the engineering-notation policy), 5 ticks kept (VTK even spacing refutes 7); a glass-card backdrop chip (`mantle` @ 0.92 + per-theme hairline `surface0`/light `surface2`, 8 px outer pad) drawn by the projection as its own 2D actor pair — NOT `SetDrawBackground`, which cannot cover the horizontal title band (E4) — with slots/containment/hit-testing moved to chip rects; banding = `LutSpec.n_colors` so field and bar band together; VTK NaN/range swatches refuted by render (they explode the content-derived box, E5) and deferred with the `box_px` accounting named. All ADR 0081 invariants preserved; no new Palette fields, no schema bump | Proposed (2026-08-04; awaits user veto; evidence = 5 rendered A/B sheets on `column_demo.h5`, live-actor mutation, harness committed in `scratchpad_shots/`) |
| [0091](0091-bernstein-consistent-load-basis.md) | Bernstein-aware consistent load reduction: `basis="lagrange"\|"bernstein"` on the field-load verbs (`g.loads.line`, `g.loads.surface.pressure/traction/shear`) — the authoring-side seam for the Ladruno Bézier control-value elements (BezierTet10/BezierTri6). The Lagrange-consistent vector applied to control values is a strongly oscillatory traction (TIMs T2 strip-footing divergence); the Bernstein branch integrates `f_a = ∫ t·B_a dΓ` in the same Gauss loop (uniform q → EQUAL `q·A/6` face loads, `q·L/3` edge loads), node order verified against the fork BezierTet10.cpp/BezierTri6.cpp. Fail-loud on tributary/element-form/quad-face combos; gravity/volume equal split documented as already Bernstein-exact; also fixes `LoadResolver.element_volume` bbox fallback for tet10/hex20 (gravity loads were ~6x off on quadratic solids); second slice = the bridge-side mismatch guard — records carry `basis` (additive column, neutral schema 2.28.0) and `build()` warns (`WarnLoadBasisMismatch`, fail-soft, exclusive-ownership interface exemption) when Lagrange-tagged loads import onto Bézier control-value nodes (incl. LadrunoUP tri6/tet10 Taylor-Hood) or Bernstein-tagged onto nodal-value nodes | Accepted (2026-08-10; live patch-test gate `test_bezier_consistent_loads_live.py` + guard gate `test_load_basis_mismatch.py`) |
| [0092](0092-partitioned-contact-emit.md) | Partitioned (MPI) contact emit: replace the blanket `BridgeError` at `apesees.py:2309` with a **locality contract** — each contact interaction is emitted inside **exactly one** rank's block (owner = majority of slave-surface nodes) with the **whole interface** ghosted onto that rank via the ADR 0027 foreign-node + SP-replay mechanism, so the fork engine needs no MPI and mortar's per-slave-node global gap reduction stays rank-local. Owner is chosen on the **master** side so the fork's `-kn auto` (which resolves the master segment's owning solid) stays native and only geometry-only slave nodes are ghosted; the partition graph weights the master surface as uncuttable. `soft=` is refused under partitioning — `m_eff` needs both surfaces' assembled mass, which no owner rule can supply. The emit half of fork **ADR-78** | Proposed (2026-08-11; adversarially reviewed same day — owner rule corrected to master-side, ghost-budget question withdrawn, read-side contract found already shipped in `f5358318`. **ADR-78 P0 ran and PASSED**: 2-rank vs serial within 1.6e−14 implicit, bit-identical explicit, 3 mutations fail as designed. New blocker for S4 — `OpenSeesMP` links `commands.cpp`, which does not register `contactSurface`, so the Tcl per-rank target cannot run contact today) |
