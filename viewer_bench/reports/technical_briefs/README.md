# technical_briefs/ — project & model architecture

**Locked:** one project brief + one brief per `models/<id>/`.  
Rules: `APE/instructions/reporting.md`.

```text
technical_briefs/
├── README.md
├── project_architecture.md
└── models/
    └── <model_id>.md
```

| Document | Answers |
|----------|---------|
| `project_architecture.md` | Habitat layout, units, toolchain, model inventory |
| `models/<model_id>.md` | Lineage role, progressive stages, links to ledger + stage reports |

Keep briefs short; depth lives in ledgers and model reports.
